package com.lastSchedule.repository;

import com.lastSchedule.entity.RedDate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RedDateRepository extends JpaRepository<RedDate, Long> {

}
